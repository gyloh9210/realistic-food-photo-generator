CURSOR_API_KEY=
PORT=8787
# Optional — Cursor model id used for all agent calls (screening, prompt-writing, generation).
# Defaults to composer-2.5 if unset.
CURSOR_MODEL=

# Agent-workflow (agent-workflow/ package)
# AGENT_WORKFLOW_ENGINEER_MODEL=composer-2.5
# AGENT_WORKFLOW_MAX_CONCURRENT=2
# AGENT_WORKFLOW_PORT=3001
# VITE_AGENT_WORKFLOW_PORT=5174
